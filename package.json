{
  "name": "jterminusweb",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p public && cp -r index.html css js *.ico *.png public/ 2>/dev/null || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["terminal", "linux", "emulator", "java"],
  "author": "Taufik Akbar",
  "license": "MIT",
  "description": "JTerminus - A lightweight Linux terminal emulator website",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongodb": "^6.20.0"
  }
}
